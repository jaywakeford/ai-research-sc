/**
 * Section Components
 * 
 * This module exports larger, composed sections of the application.
 * These components represent complete sections that can be used to build full pages.
 */

/**
 * MetricsSection - A component for displaying key performance metrics and statistics.
 * Features:
 * - Grid layout for multiple metrics
 * - Animated number displays
 * - Responsive design for different screen sizes
 * - Visual representations of data
 * 
 * Usage:
 * Import and place directly in page layouts where metrics need to be displayed.
 * The component handles its own data fetching and state management.
 */
export { default as MetricsSection } from './MetricsSection'; 